{% extends 'base.html.twig' %}

{% block title %}Tags{% endblock %}

{% block body %}
    <h1>Tags: </h1>
    <ul>
        {% for tag in tags %}
            <li><a href="{{ path('tag_showOne', {"name": tag.name}) }}">{{ tag.name }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}
