{% extends 'base.html.twig' %}

{% block title %}Bienvenue{% endblock %}

{% block body %}
    <h1>Hello World</h1>

    <ul>
        {% for category in catagories %}
            <li><a href="{{ path('category_showAllArticles', {'id' : category.id}) }}">{{ category.name }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}
